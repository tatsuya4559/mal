; vim: ft=scheme
(defmacro! defun!
  (fn* (fname args body)
       `(def! ~fname (fn* ~args ~body))))

(defun! not (x)
  (if x false true))

(defun! nth (lst n)
  (if (= n 0)
    (car lst)
    (nth (cdr lst) (- n 1))))
